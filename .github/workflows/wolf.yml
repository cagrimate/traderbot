name: Wolf Crypto Bot 🐺

on:
  # 1. ZAMANLAYICI: Her 20 dakikada bir çalıştır (API kotasını korumak için ideal)
  schedule:
    - cron: '*/10 * * * *'
  
  # 2. MANUEL TETİK: İstersen butona basıp elle çalıştırabilmen için
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # Maksimum çalışma süresi (Bot takılırsa 10 dk sonra zorla kapat)
    timeout-minutes: 10

    steps:
      - name: Kodları Çek (Checkout)
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: WolfBot'u Çalıştır
        env:
          # GitHub kasasındaki şifreleri bota veriyoruz
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          BINANCE_API_KEY: ${{ secrets.BINANCE_API_KEY }}
          BINANCE_SECRET_KEY: ${{ secrets.BINANCE_SECRET_KEY }}
        run: python main.py
